name: contact_management
description: An app that manages the contacts of it users.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.1.1

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  shared_preferences: ^2.0.18

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true